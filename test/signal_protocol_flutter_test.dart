import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Signal Protocol package loads correctly', () {
    // Basic test to ensure the package can be imported
    expect(true, isTrue);
  });
}
